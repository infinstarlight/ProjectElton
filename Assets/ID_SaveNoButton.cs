using UnityEngine.UI;
using UnityEngine;

public class ID_SaveNoButton : MonoBehaviour
{
    public Button noButton;
    // Start is called before the first frame update
    void Start()
    {
        noButton = GetComponent<Button>();
    }

   
}
