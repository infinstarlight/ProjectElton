using UnityEngine.UI;
using TMPro;
using UnityEngine;

public class ID_PlayerMoney : MonoBehaviour
{
    public TMP_Text textObject;
    // Start is called before the first frame update
    void Start()
    {
        textObject = GetComponent<TMP_Text>();
    }

   
}
