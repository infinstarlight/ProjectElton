using UnityEngine.UI;
using UnityEngine;

public class ID_SaveYesButton : MonoBehaviour
{
    public Button yesButton;
    // Start is called before the first frame update
    void Start()
    {
        yesButton = GetComponent<Button>();
    }

}
